#post_content
	%h1= @post.title
	%p= @post.content

	= link_to "Edit", edit_post_path(@post)
	= link_to "Delite", post_path(@post), method: :delete,
						data: { confirm: "Are you shure you want to do this?"}
	= link_to "Home", root_path